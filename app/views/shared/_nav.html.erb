<nav class="navbar justify-content-end bg-light">
  <ul class="nav">
    <% if user_signed_in? %>
      <li class="nav-item">
        <button type="button" class="btn btn-default navbar-btn">
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-dark", style: "text-decoration: none" %>
        </button>
      </li>
    <% else %>
      <li class="nav-item">
        <button type="button" class="btn btn-default navbar-btn">
          <%= link_to "Log In", new_user_session_path, method: :get, class: "text-dark", style: "text-decoration: none" %>
        </button>
      </li>
      <li class="nav-item">
        <button type="button" class="btn btn-default navbar-btn">
          <%= link_to "Sign Up", new_user_registration_path, method: :get, class: "text-dark", style: "text-decoration: none" %>
        </button>
      </li>
    <% end %>

  </ul>
</nav>
